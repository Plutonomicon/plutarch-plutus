\a b ->
  case 
    (EqualsInteger # a # (0 :: Integer))
    [ (\c ->
        (\mArg_j -> mArg_j # mArg_j)
        # (\functionalArg_k ->
            (\d e f ->
              case 
                (EqualsInteger # f # (0 :: Integer))
                [ (\g -> d # g # (ModInteger # e # g)) # f
                , (\h ->
                    case 
                      (LessThanEqualsInteger # h # (0 :: Integer))
                      [h, (\i -> SubtractInteger # (0 :: Integer) # i) # h]
                  )
                  # e
                ]
            )
            # (functionalArg_k # functionalArg_k)
          )
        # c
        # b
      )
      # a
    , b
    ]